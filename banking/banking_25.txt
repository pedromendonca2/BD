/* Escreva uma consulta que retorna o nome dos clientes (customer_name) que possuem empréstimo em agências em
que o cliente chamado 'Hayes' possui empréstimo. O resultado não deve conter ocorrências duplicadas, nem incluir o nome do referido cliente. */

HayesBranches = pi branch_name → HayesBranch (sigma customer_name = 'Hayes' (loan ⨝ borrower))

pi customer_name (sigma loan.branch_name = HayesBranches.HayesBranch ∧ customer_name ≠ 'Hayes' ((loan ⨝ borrower) ⨯ HayesBranches))
/* Escreva uma consulta para obter a quantidade média de equipes (tabela constructors) por temporada (tabela
seasons) e retorne o resultado arredondado para um número inteiro (por ex., 32.5 → 33; 40.2 → 40). A coluna da
tabela de saída deve ter o nome MediaEquipes. */

Retorno = π constructorId, year (races ⨝ results)
Contador = γ year; count(constructorId) → c (Retorno)
MediaCrua = γ; avg(c) → a (Contador)
Media = π round(a) → MediaEquipes (MediaCrua)
Media